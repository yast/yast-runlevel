/**
 * YaST2 test case for runlevel editor
 *
 * tests function isDisabled
 */
{
    import "Runlevel";
    include "testsuite.ycp";

    DUMP (Runlevel::isDisabled ($[]));
    DUMP (Runlevel::isDisabled (
        $[
            "start" : [],
	    "defstart" : ["1",],
        ]
        ));
    DUMP (Runlevel::isDisabled (
        $[
            "start" : [],
            "defstart" : [],
        ]
        ));
    DUMP (Runlevel::isDisabled (
        $[
            "start" : [ "1", ],
        ]
        ));
}
