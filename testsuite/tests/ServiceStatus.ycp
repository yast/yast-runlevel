
{
    import "Runlevel";
    include "testsuite.ycp";

    map READ = $[];
    map EXEC = $[];

    READ = $[ "init" : $[ "scripts" : $[ "exists" : true, ] ] ];
    EXEC = $[ "target" : $[ "bash" : 3 ] ];

    TEST (``(Runlevel::ServiceStatus ("aaa")), [ READ, $[], EXEC ], nil);
}
